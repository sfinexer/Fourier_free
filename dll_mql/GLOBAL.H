// описание глабальных переменных
// данные переменные описываются один раз, они автоматом идут во все подпрограммы
double  *Open, *High, *Low, *Close;
__int64 *Time;
double   *Renco_out;
int      *Renco_kol;  // счетчик сопоставления реальных свечей со свечами ренко
__int64  *Renco_time;
int      kol_blokov;  // количество блоков данных которые прочитаны последний раз
// функция инициализации
int inite;  // флаг что прошла инициализация
int vrem_kol;  // временное значение для вычислений
int time_poias;  // часовой пояс
int smesh; // смещение по группам (сколько последних групп данных читать)
int fourier_garm;  // количество гармоник для фурье
double FreqTOL;  // погрешность фурье 0.000005;
double shag_renco;   // шаг по ренко барам
__int64 sizefile;  // Количество данных которые прочитали последний раз.  (не размер файла данных)
string GO[400][10];  // Массив файла ###go
string put_prog;   // папка где лежит данный файл
